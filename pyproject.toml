[project]
name = "bangumi-pages"
version = "0.1.0"
description = "Zensical site for bangumi-pages"
readme = "readme.md"
requires-python = ">=3.10"
dependencies = [
    "zensical==0.0.55",
]

[tool.uv]
package = false
